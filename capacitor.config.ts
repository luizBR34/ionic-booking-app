import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemy.ionic',
  appName: 'ionic-angular-course-02',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
